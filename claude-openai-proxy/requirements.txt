# --- 웹 서버 ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- 설정/검증 ---
pydantic>=2.6
pydantic-settings>=2.2

# --- Claude Code SDK (OAuth 재사용) ---
# 내부적으로 `claude` CLI(@anthropic-ai/claude-code)를 호출한다.
# CLI 는 별도로 npm 으로 설치하고 `claude login` 이 필요하다(README 참고).
claude-agent-sdk>=0.1.0

# --- 테스트 ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
